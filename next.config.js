/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['i.ebayimg.com', 'i.postimg.cc'], // Agrega aquí los dominios permitidos para las imágenes
    },
  };
  

module.exports = nextConfig
